[project]
name = "wp-data-reader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ryan Lane", email = "futile@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "lxml>=6.1.3",
    "markdownify>=1.2.3",
    "textual>=8.2.8",
]

[project.scripts]
wp-data-reader = "wp_data_reader.cli:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
